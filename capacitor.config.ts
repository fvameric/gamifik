import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gamifik.app',
  appName: 'gamifik22',
  webDir: 'dist/gamifik22',
  bundledWebRuntime: false
};

export default config;
